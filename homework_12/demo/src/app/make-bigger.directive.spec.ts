import { MakeBiggerDirective } from './make-bigger.directive';

describe('MakeBiggerDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeBiggerDirective();
    expect(directive).toBeTruthy();
  });
});
